

# مسیر فایل
file_path = r"D:\قالبسازی.xlsx"

# d:\قالب سازی .xlsx
if not os.path.exists(file_path):
    print("❌ فایل یافت نشد. لطفاً مسیر را بررسی کنید.")
else:
    # خواندن همه شیت‌ها
    excel_data = pd.read_excel(file_path, sheet_name=None)

    print("✅ فایل با موفقیت خوانده شد.")
    print(f"📘 تعداد شیت‌ها: {len(excel_data)}\n")

    # پیمایش میان همه شیت‌ها
    for sheet_name, df in excel_data.items():
        print("=" * 70)
        print(f"📄 نام شیت: {sheet_name}")
        print(f"📊 تعداد ردیف‌ها: {df.shape[0]} | تعداد ستون‌ها: {df.shape[1]}")
        print("🧩 نام ستون‌ها:", list(df.columns))
        
        # نمایش 5 سطر اول داده‌ها
        print("\nنمونه داده‌ها:")
        print(df.head())
        print("=" * 70, "\n")

    print("✅ گزارش کامل از تمام شیت‌ها تولید شد.")

